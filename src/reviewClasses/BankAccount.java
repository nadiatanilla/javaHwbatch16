package reviewClasses;

public class BankAccount {
    String accountHolder;
    long accountNumber;
    double accBalance;
    int ssn;// instance variable are inside the class but outside the method
    // local inside the method
    static String BnkName="BOA"; // does  nt get any coppy it is a class variable
}
